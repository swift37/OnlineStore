@model IEnumerable<OnlineStore.Domain.Product>

<div class="products main">
    @if (Model.Count() > 0)
    {
        foreach (var product in Model)
        {
            @await Component.InvokeAsync("ProductCard", new { product = product })
        }
    }
</div>